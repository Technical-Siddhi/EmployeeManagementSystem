{
  "name": "employee-attendance-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm --prefix frontend start\" \"npm --prefix backend run dev\" --kill-others --success first",
    "backend": "npm --prefix backend run dev",
    "frontend": "npm --prefix frontend start",
    "install-all": "npm install --legacy-peer-deps && npm --prefix frontend install --legacy-peer-deps && npm --prefix backend install --legacy-peer-deps",
    "build": "npm --prefix frontend run build",
    "test": "npm --prefix frontend run test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
